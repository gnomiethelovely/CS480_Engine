/*
    Webservice for the workflow engine.
    Calls webservice(to be defined later) and returns a value.

    Gets called when Node wants to call service.
*/

public class WebService_WFEngine {
    int value;
    public WebService_WFEngine() {
        /*
        Define this class more later when we have more infomation for what
        purpose this has.
        */
    }
    
    public int get_Value() {
        return value;
    }
    
}
